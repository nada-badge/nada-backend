const COMMUNITY = require('./community');

const field = COMMUNITY.field;

const region = COMMUNITY.region;

const inActivity = ["공모전", "지역/연합 동아리", "교내 동아리", "소모임", "프로젝트", "대외활동", "인턴"];

module.exports = {
    field,
    region,
    inActivity
}                